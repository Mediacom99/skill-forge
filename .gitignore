# Internal working notes (handoff between sessions; not for the public repo)
SESSION-NOTES.md

# Internal commercialization / strategy research — kept local, not public
docs/commercialization-research-2026-07.md
ROADMAP-private.md

# Drift report produced by check-sources (ephemeral; never committed)
drift.md

# Prompts saved by the skill's "save to file" delivery (the default ./prompts/ dir) — user output, not repo content
prompts/

# Clipboard scratch file from the skill's copy-to-clipboard delivery (emptied after copy; never committed)
.skill-forge-clipboard.txt

# Claude Code local state (personal; not repo content)
.claude/settings.local.json
.claude/memory/

# OS / editor cruft
.DS_Store
*.swp
__pycache__/
*.pyc
